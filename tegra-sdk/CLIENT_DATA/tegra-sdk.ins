[Initial]
Message=Tegra SDK Installation
StayOnTop=false

[Aktionen]
DefVar $InstallDir$
Set $InstallDir$ = "D:\sdks\tegra042"

Winbatch_install_sdk

if not(FileExists($InstallDir$))
    logError "Fatal: After installation "+$InstallDir$+" not found"
    isFatalError
endif

[Winbatch_install_sdk]
%SCRIPTPATH%\tegra.msi /qb INSTALLDIR=$InstallDir$ ALLUSERS=1
